<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.samsung.os">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-feature android:name="android.hardware.camera2" />

        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

        <activity android:name=".Log_In">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Sign_Up" />
        <activity android:name=".Find_ID_Password" />
        <activity android:name=".Main_page" />
        <activity android:name=".MoneyBook" />
        <activity android:name=".SelMoneyBook" />
        <activity android:name=".sns_list" />
        <activity android:name=".sns_text_list" />
        <activity android:name=".Upload_Sns" />
        <activity android:name=".listview_text"></activity>
    </application>

</manifest>